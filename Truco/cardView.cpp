#include "pch.h"
#include "cardView.h"

cardView::cardView()
{

}

cardView::cardView(CFrameWnd* parent)
{
	//button.Create(_T("Clique"), WS_CHILD | WS_VISIBLE, CRect(10, 10, 100, 30), this, 1);
	//valueTextfield.Create(es_multiline | ws_child | ws_visible | ws_tabstop | ws_border, crect(10, 40, 100, 60), parent, 1);
	//suittextfield.create(ES_MULTILINE | WS_CHILD | WS_VISIBLE | WS_TABSTOP | WS_BORDER, CRect(10, 40, 100, 60), parent, 2);
}
//
void cardView::displayCard(card c) const
{
	//CString sWindowText;
	//CString sWindowText2;
	//valueTextfield.GetWindowText(sWindowText);
	//suitTextfield.GetWindowText(sWindowText2);
	//AfxMessageBox(_T("Carta selecionada: " + sWindowText + " " + sWindowText2));
}
